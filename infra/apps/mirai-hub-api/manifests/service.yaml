apiVersion: v1
kind: Service
metadata:
  name: mirai-hub-api
  namespace: mirai-hub
spec:
  selector:
    app.kubernetes.io/name: mirai-hub-api
  ports:
    - name: http
      port: 8000
      targetPort: http
