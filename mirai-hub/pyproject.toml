[project]
name = "mirai-hub"
version = "0.2.0"
description = "Mirai Hub — Layer 5 chat UI (Chainlit)"
requires-python = ">=3.12"
dependencies = [
    "chainlit[custom-data]>=2.12.0",
    "openai>=2.0.0",
    "mcp>=1.28.1",
    "pydantic-settings>=2.14.2",
]
